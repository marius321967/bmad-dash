{
  "name": "bmad-dash",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/bmad-dash/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/apps/bmad-dash"
      ],
      "options": {
        "command": "node build.mjs",
        "cwd": "apps/bmad-dash"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint .",
        "cwd": "apps/bmad-dash"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/bmad-dash/tsconfig.spec.json"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "options": {
        "jestConfig": "apps/bmad-dash/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "build"
      ],
      "options": {
        "command": "vsce package --no-dependencies --allow-missing-repository --baseImagesUrl https://raw.githubusercontent.com/gv8-control/bmad-playground/HEAD/apps/bmad-dash",
        "cwd": "dist/apps/bmad-dash"
      }
    }
  },
  "tags": []
}
